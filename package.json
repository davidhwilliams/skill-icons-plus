{
  "name": "skill-icons",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "node build.js",
    "build": "node build.js",
    "deploy": "node build.js && gh-pages -d dist",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "prettier": "^3.3.3"
  }
}
