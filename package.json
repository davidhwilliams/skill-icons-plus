{
  "name": "skill-icons-plus",
  "version": "2.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "index": "tsx generate-icon-table.ts && pnpm run format",
    "dev": "tsx build.ts && miniflare --watch",
    "build": "tsc && tsx build.ts",
    "deploy": "tsx build.ts && gh-pages -d dist",
    "deploy:cloudflare": "tsx build.ts && wrangler publish",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.16.3",
    "gh-pages": "^6.3.0",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "wrangler": "^4.36.0"
  }
}
